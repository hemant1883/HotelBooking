spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_booking_db?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    # REMOVE THE DIALECT LINE ENTIRELY